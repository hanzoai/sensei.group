
import React from "react";

interface PerspectiveRaysProps {
  lineCount?: number;
}

const PerspectiveRays: React.FC<PerspectiveRaysProps> = () => {
  // Empty component - no rays to render
  return null;
};

export default PerspectiveRays;
