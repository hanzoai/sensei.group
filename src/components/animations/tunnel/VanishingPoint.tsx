
import React from "react";
import { motion } from "framer-motion";

const VanishingPoint: React.FC = () => {
  return (
    <div className="hz-center-xy hz-abs hz-z-raised">
      {/* Vanishing point without glow */}
    </div>
  );
};

export default VanishingPoint;
